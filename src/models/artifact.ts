/*
* Self contained asset this is a runnable code-bundle. Uniquely identified amongst other artifacts
* of like type.
*/
class Artifact {
  constructor(name: string, version: number, history: any) {}
}